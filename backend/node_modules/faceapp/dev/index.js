const faceapp = require('../src')

const { writeFileSync, readFileSync } = require('fs')

const main = async () => {
  // const resp = await faceapp.listFilters()
  // writeFileSync('./dev/aaa.json', JSON.stringify(resp))

  const resp = JSON.parse(readFileSync('./dev/aaa.json'))
  
  console.log(noDupes.map(x => x.id))
}
main()
